# E_RENDER_PIPELINE_INVALID_PASS_STATE

系统违反 render pass ownership。检查 shared/isolated pass 声明与 begin/end 边界。

所有结构化错误都包含 domain、code、recoverable、recovery、context、path 和可选 cause；诊断时保留完整对象，不要只记录 message。
