export const EDITOR_LARGE_SCENE_BUDGETS = Object.freeze({
  1000: Object.freeze({
    interactionP95Ms: 75,
    maxLongTaskCount: 0,
    maxLongTaskMs: 50,
    domNodes: 1200,
    heapBytes: 96 * 1024 * 1024,
    loadMs: 250,
    prepareEntitiesMs: 20,
    commitMs: 30,
    serializationMs: 500,
    exportMs: 1500,
    playStartupMs: 1800,
    restartMs: 350,
  }),
  10000: Object.freeze({
    interactionP95Ms: 75,
    maxLongTaskCount: 0,
    maxLongTaskMs: 50,
    domNodes: 1200,
    heapBytes: 256 * 1024 * 1024,
    loadMs: 250,
    prepareEntitiesMs: 50,
    commitMs: 50,
    serializationMs: 1000,
    exportMs: 2500,
    playStartupMs: 2000,
    restartMs: 350,
  }),
});
