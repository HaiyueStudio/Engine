# Engine gate fixtures

`lighting-content/pad-simulator/scenes/billiards-3d-import.scene.json` is a byte-identical snapshot of HaiyueStudio Games `games/pad-simulator/scenes/billiards-3d-import.scene.json` at d62af1580ef5c897f3fa56564c9fdf49344954ab.
It preserves the existing 363,097-byte lighting workload and SHA-256 `9e7f393aba90a91a1a84a42be9583ce627bfa2a0ee996c0b843d21f9514ba007`. The HTTP mount stays `/games`, preserving recorded source identity. No Games checkout is required to replay Engine's lighting gate.

Changing this fixture requires a reviewed workload/evidence update, not a budget adjustment.
`engine-consumers/` contains isolated npm consumer fixtures; the UI button consumer is owned by the UI repository.
