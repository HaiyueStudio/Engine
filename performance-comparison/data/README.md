# Comparison data

`local/*.json` contains candidate reports produced on the current machine and is ignored by Git. A report records the real OS, CPU, browser, adapter/backend, exact dependency versions, source revision, workload, raw samples, structural parity and visual sanity result.

Formal reviewed evidence is not written here automatically. G07 may promote a clean-revision full report only after numeric and visual review.

